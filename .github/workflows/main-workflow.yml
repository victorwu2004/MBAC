name: Workflow

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
  workflow_dispatch:
  # schedule:
    # - cron: '0 0 * * 1,3'

jobs:
  build:
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        

